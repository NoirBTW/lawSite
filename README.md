# LawSite - Юридический сайт

Современный адаптивный веб-сайт для юридической компании с автоматической прокруткой к началу страницы при навигации.

## 🚀 Основные возможности

- **Автоматическая прокрутка** - при переходе между страницами пользователь всегда оказывается в начале новой страницы
- **Полностью адаптивный дизайн** - оптимизирован для всех устройств
- **Современный UI/UX** - интуитивный интерфейс с плавными анимациями
- **React 19** - последняя версия React с современными возможностями
- **Vite** - быстрая сборка и разработка

## 📱 Адаптивность

Сайт автоматически адаптируется под различные размеры экранов:

- **Desktop** (1200px+) - полная версия с боковой навигацией
- **Tablet** (768px-1199px) - адаптированная версия для планшетов
- **Mobile** (до 767px) - мобильная версия с гамбургер-меню

## 🏗️ Структура проекта

```
src/
├── components/          # Переиспользуемые компоненты
│   ├── Header.jsx      # Шапка сайта с навигацией
│   ├── Footer.jsx      # Подвал сайта
│   ├── ScrollToTop.jsx # Автоматическая прокрутка к началу страницы
│   └── *.css           # Стили компонентов
├── pages/              # Страницы сайта
│   ├── Home.jsx        # Главная страница
│   ├── Services.jsx    # Услуги
│   ├── Team.jsx        # Команда
│   ├── Reviews.jsx     # Отзывы
│   ├── Contacts.jsx    # Контакты
│   └── *.css           # Стили страниц
├── App.jsx             # Главный компонент с роутингом
├── App.css             # Глобальные стили
└── main.jsx            # Точка входа
```

## 🎯 Компонент ScrollToTop

Компонент `ScrollToTop` автоматически прокручивает страницу к началу при переходе между маршрутами, решая проблему с позицией прокрутки при навигации.

### Использование

```jsx
import ScrollToTop from "./components/ScrollToTop";

function App() {
  return (
    <Router>
      <div className="App">
        <ScrollToTop />
        {/* Остальные компоненты */}
      </div>
    </Router>
  );
}
```

### Параметры

- `delay` (number, по умолчанию: 0) - Задержка перед прокруткой в миллисекундах
- `excludePaths` (string[], по умолчанию: []) - Массив путей, для которых прокрутка не выполняется
- `smooth` (boolean, по умолчанию: true) - Использовать ли плавную прокрутку
- `debug` (boolean, по умолчанию: false) - Включить подробное логирование для диагностики

### Примеры

```jsx
// Базовая прокрутка
<ScrollToTop />

// С задержкой 200ms
<ScrollToTop delay={200} />

// Без плавной прокрутки
<ScrollToTop smooth={false} />

// Исключить определенные пути
<ScrollToTop excludePaths={['/admin', '/dashboard']} />

// Включить режим отладки
<ScrollToTop debug={true} />

// Комбинированные настройки
<ScrollToTop
  delay={100}
  smooth={true}
  excludePaths={['/admin']}
  debug={true}
/>
```

### Особенности

- Автоматически срабатывает при изменении маршрута
- Поддерживает плавную прокрутку с fallback для старых браузеров
- Не рендерит никакого UI
- Легко настраивается под нужды проекта
- Умная логика для определения реальных переходов между страницами
- Поддержка задержек для лучшего UX
- Возможность исключения определенных маршрутов
- Автоматическое определение поддержки smooth scroll API

## 🎨 Дизайн

### Цветовая схема

- **Основной цвет**: #2c5aa0 (синий)
- **Акцентный цвет**: #ffd700 (золотой)
- **Фон**: #f8f9fa (светло-серый)
- **Текст**: #333 (темно-серый)

### Типографика

- **Заголовки**: Segoe UI, Roboto, системные шрифты
- **Основной текст**: системные шрифты с оптимизацией для экранов

## 🚀 Запуск проекта

### Требования

- Node.js 18+
- npm или yarn

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Сайт будет доступен по адресу: http://localhost:5173

### Сборка для продакшена

```bash
npm run build
```

### Предварительный просмотр сборки

```bash
npm run preview
```

## 📄 Страницы сайта

### 1. Главная страница (/)

- Hero секция с призывом к действию
- Основные услуги компании
- Опыт и партнеры
- FAQ секция
- Формы заявок на консультацию

### 2. Услуги (/services)

- Детальное описание всех услуг
- Категории: семейное, уголовное, гражданское, военное право
- Призыв к действию

### 3. Команда (/team)

- Информация о юристах компании
- Опыт и специализация каждого
- Сертификаты и награды

### 4. Отзывы (/reviews)

- Отзывы клиентов
- Статистика компании
- Возможность оставить отзыв

### 5. Контакты (/contacts)

- Контактная информация
- Форма обратной связи
- Адрес и реквизиты компании

## 🔧 Технические детали

### Роутинг

Используется React Router v6 для навигации между страницами с автоматической прокруткой к началу.

### Стили

- CSS модули для компонентов
- Адаптивные медиа-запросы
- CSS Grid и Flexbox для макетов
- Плавные переходы и анимации
- Автоматическая плавная прокрутка

### Формы

- Валидация на стороне клиента
- Адаптивные формы для всех устройств
- Модальные окна для заявок

## 📱 Мобильная оптимизация

- **Touch-friendly** интерфейс
- **Гамбургер-меню** для мобильных устройств
- **Оптимизированные формы** для сенсорных экранов
- **Адаптивные изображения** и контент

## 🌐 Браузерная поддержка

- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+

## 📝 Лицензия

Проект разработан для юридической компании.

## 👨‍💻 Разработчик

Сайт разработан с использованием современных веб-технологий.

---

**LawSite** - профессиональная юридическая помощь!

## 🔧 Решение проблем

### Проблема с плавной прокруткой

Если плавная прокрутка не работает на определенных страницах, это может быть связано с CSS стилями, которые мешают прокрутке.

#### Возможные причины:

1. **CSS свойства `min-width: 100vw`** - могут создавать горизонтальную прокрутку
2. **Отрицательные отступы `margin-left: calc(-50vw + 50%)`** - могут конфликтовать с прокруткой
3. **Проблемы с `overflow`** - могут блокировать прокрутку

#### Решение:

Компонент `ScrollToTop` автоматически прокручивает страницу к началу при каждом изменении маршрута.

#### Диагностика:

Откройте консоль браузера (F12) и перейдите между страницами. Вы должны увидеть логи:

```
ScrollToTop: выполнение прокрутки для пути: /reviews
ScrollToTop: использование плавной прокрутки
ScrollToTop: прокрутка выполнена успешно
```

Если логи не появляются, проверьте:

1. Правильно ли импортирован компонент `ScrollToTop`
2. Нет ли ошибок в консоли браузера
3. Правильно ли настроены маршруты в React Router
4. Включен ли debug режим: `<ScrollToTop debug={true} />`

#### Включение режима отладки:

Для детальной диагностики включите debug режим:

```jsx
<ScrollToTop debug={true} />
```

Это покажет подробную информацию о:

- Выполнении прокрутки для каждого маршрута
- Типе используемой прокрутки
- Успешности выполнения

#### Ручное исправление CSS:

Если проблема сохраняется, проверьте CSS файлы страниц на наличие проблемных стилей:

```css
/* ПРОБЛЕМНЫЕ СТИЛИ - удалить или закомментировать */
.min-width: 100vw;
margin-left: calc(-50vw + 50%);
margin-right: calc(-50vw + 50%);

/* ЗАМЕНИТЬ НА */
overflow-x: hidden;
position: relative;
```

#### Альтернативное решение:

Если ничего не помогает, можно временно отключить плавную прокрутку для проблемной страницы:

```jsx
<ScrollToTop
  smooth={pathname !== "/reviews"} // Отключаем плавную прокрутку для Reviews
  delay={100}
/>
```

## 🧪 Тестирование

### Проверка работы ScrollToTop

1. **Запустите проект в режиме разработки:**

   ```bash
   npm run dev
   ```

2. **Откройте консоль браузера (F12)**

3. **Переходите между страницами** и следите за логами в консоли

4. **Ожидаемые логи:**

   ```
   ScrollToTop: выполнение прокрутки для пути: /
   ScrollToTop: использование плавной прокрутки
   ScrollToTop: прокрутка выполнена успешно
   ```

5. **Для страницы Reviews:**
   ```
   ScrollToTop: специальная обработка для страницы Reviews
   ScrollToTop: выполнение прокрутки для Reviews
   ```

### Проверка плавной прокрутки

1. **Прокрутите страницу вниз**
2. **Перейдите на другую страницу**
3. **Убедитесь, что страница автоматически прокрутилась к началу**

### Проверка на мобильных устройствах

1. **Откройте DevTools (F12)**
2. **Включите режим мобильного устройства**
3. **Проверьте работу прокрутки на разных размерах экрана**

## 🧪 Быстрое тестирование

### Проверка за 30 секунд:

1. **Запустите проект:** `npm run dev`
2. **Откройте консоль браузера (F12)**
3. **Перейдите на любую страницу** (например, `/services`)
4. **Прокрутите страницу вниз**
5. **Перейдите на страницу Reviews** (`/reviews`)
6. **Проверьте консоль** - должны появиться логи прокрутки

### Ожидаемый результат:

- Страница автоматически прокручивается к началу
- В консоли появляются логи о выполнении прокрутки
- Для страницы Reviews используется специальная задержка

### Если не работает:

1. Проверьте, что в консоли нет ошибок
2. Убедитесь, что `debug={true}` включен в App.jsx
3. Проверьте, что все CSS проблемы исправлены
